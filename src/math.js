// this is meant to be used with the import keyword

const add = (x, y) => x + y;
const subtract = (x, y) => y - x;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;

export default {add, subtract, multiply, divide}